import { ReactNode } from 'react';

export const metadata = {
    title: 'Coordinator Scanner - Event QR System',
};

export default function CoordinatorLayout({ children }: { children: ReactNode }) {
    return children;
}
